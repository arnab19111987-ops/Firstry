[pytest]
filterwarnings =
    error::DeprecationWarning
    ignore:.*FirstTryConfig.*:DeprecationWarning:tests/test_config_compat\.py

# Note: most pytest settings live in pyproject.toml under [tool.pytest.ini_options].
# This file only configures filterwarnings to make the FirstTryConfig alias a
# test-time error except in the compat test.

# Warm cache directories for testmon and coverage
testmon_cache_dir = .firsttry/warm/testmon
