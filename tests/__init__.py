"""Make `tests` a package so tests can import `tests.*` modules during collection.

This file is intentionally empty; it only exists to enable package-style imports
from within test modules like `from tests.cli_utils import ...`.
"""
