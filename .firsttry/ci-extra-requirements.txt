# Extra packages required by local CI parity runs and containerized parity
# Keep this file small — it's installed into the parity/container environment.

# TOML parser used by ci_parity.checker
toml>=0.10
# Add any test-only deps your repo needs; examples below
boto3>=1.34.0
jsonschema>=4.0
blake3>=0.4
moto>=5.0.0
types-PyYAML>=6.0.12
