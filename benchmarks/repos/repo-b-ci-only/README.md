# CI Project (Has CI + Linter Config)

This project has CI workflows and linter configuration.
Used for testing CI parity and detection of CI-based workflows.

## Purpose
- Test --source ci detection and parity
- Validate CI workflow parsing
- Compare CI vs detect modes